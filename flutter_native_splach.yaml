flutter_native_splash:
  color: "#0078d7"
  image: assets/ios_splach.png

  android_12:
    image: assets/splach2.png
    color: "#0078d7"
    
  # Keep splash until manually removed
  android_gravity: center
  ios_content_mode: center
  
  # Ensure splash stays until Flutter removes it
  android_disable_fullscreen: false